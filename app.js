async function load(){const r=await fetch('/api/surveys');const s=await r.json();const card=x=>`<div class="survey card"><div><strong>${x.title}</strong><div class="muted">${x.minutes} min • ${x.provider}</div></div><strong class="reward">${x.reward}</strong></div>`;document.getElementById('preview').innerHTML=s.slice(0,3).map(card).join('');document.getElementById('surveys').innerHTML=s.map(card).join('')}load().catch(console.error);